# PAM password configuration
# Password management for local authentication

password    required     pam_unix.so sha512 shadow nullok try_first_pass use_authtok
password    optional     pam_permit.so