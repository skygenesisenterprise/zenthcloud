# Container access control configuration
# Additional security restrictions for container environment

# Allow only these users to access via SSH
+:ssh-user:ALL
+:mailer:LOCAL

# Deny all other users (blacklist approach)
-:ALL:ALL

# Allow specific services for ssh-user
+:ssh-user:sshd
+:ssh-user:mailer

# Network access restrictions (if implemented)
#+:ssh-user:192.168.1.0/24
#+:mailer:127.0.0.1

# Time restrictions (if needed)
#+:ssh-user:Al0800-1700